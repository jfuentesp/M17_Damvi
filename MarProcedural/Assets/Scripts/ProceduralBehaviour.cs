using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProceduralBehaviour
{


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*public float CalculatePerlinNoise(float x, float y, float frequency, int width, int height, float offsetX = 0, float offsetY = 0,
            int octaves = 0, float lacunarity = 2, float persistence = 0.5f, bool carveOctaves = true,
            bool verbose = false, bool returnAllValues = false)
    {

    }*/
}
